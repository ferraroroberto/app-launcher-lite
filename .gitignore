# --- Python ---------------------------------------------------------------
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Virtualenvs ----------------------------------------------------------
.venv/
venv/
env/

# --- Node (Vitest harness) ------------------------------------------------
node_modules/

# --- Runtime state --------------------------------------------------------
.tray.pid
*.log

# --- Editor / OS ----------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- Config overrides (keep the committed templates) ---------------------
config/config.json
config/webapp_config.json
config/apps.json
config/jobs.json
config/webauthn_devices.json

# --- Webapp runtime data --------------------------------------------------
webapp/last_tunnel_url.txt
webapp/certificates/
webapp/cloudflared.yml
webapp/terminal-themes.json
webapp/sessions/
webapp/jobs/
# e2e-gate temp copy of the real config (issue #441) — carries auth secrets
webapp/e2e-autoboot-webapp-config.json

# --- Interactive-terminal scratch (image uploads land here) --------------
.launcher-tmp/
